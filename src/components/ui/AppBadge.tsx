import Badge from 'react-bootstrap/Badge'

/**
 * Badge padrão. Hoje é o componente do Bootstrap; quando o visual fechar,
 * personalize aqui sem trocar imports nas telas.
 */
export const AppBadge = Badge
