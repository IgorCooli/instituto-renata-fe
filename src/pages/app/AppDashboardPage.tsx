import { PageContainer, PageHeader } from '../../components/layout'

export function AppDashboardPage() {
  return (
    <PageContainer>
      <PageHeader
        title="Início"
        description="Área logada com sessão mock (features e papel). Use o menu para abrir os módulos."
      />
    </PageContainer>
  )
}
