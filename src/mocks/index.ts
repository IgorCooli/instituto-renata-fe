/**
 * Dados fictícios compartilhados — substituir por chamadas ao backend quando existir.
 * Ver docs/SPEC.md §2.1.
 */
export const mockAppName = 'Gestão de consultório'
export const mockTagline =
  'Ambiente em construção — visualização com dados mockados até integração com o API.'

/** Marca no painel logado (tela de início / dashboard). */
export const mockBrandDisplayName = 'Instituto Dra. Renata Jorge'
export const mockDashboardTagline = 'Gerencie sua clínica com excelência'
